apiVersion: v1
kind: Service
metadata:
  name: nginx-service
  annotations:
    metallb.universe.tf/allow-shared-ip: sharing key
spec:
  selector:
    app: nginxs
  type: LoadBalancer
  ports:
  - port: 5000
    targetPort: 80
    protocol: TCP
  